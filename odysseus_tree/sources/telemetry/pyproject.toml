[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "telemetry"
version = "0.1"
dependencies = [
  "protobuf",
  "psutil",
  "gmqtt",
]
